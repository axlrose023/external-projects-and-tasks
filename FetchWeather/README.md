
## Quick Start

1. **Create .env file:**
```bash
cp env.example .env
```

2. **Set environment variables in .env:**
```env
DB_HOST=postgres
DB_PORT=5432
DB_USER=user
DB_PASSWORD=password
DB_NAME=db
```

3. **Run application:**
```bash
docker-compose up -d
```
